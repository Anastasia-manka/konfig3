```
def users = {
  {q(John Doe), 30, q(Developer)},
  {q(Jane Smith), 25, q(Designer)},
  {q(Peter Jones), 40, q(Manager)}
}
def team_size = |users.length|
' Вычисляем размер команды
--[[
Многострочный комментарий, описывающий 
сложную логику обработки пользователей
]]
def new_user = {q(Alice Brown), 28, q(Analyst)}
def updated_users = {users, new_user}
def final_team_size = |updated_users.length|
```